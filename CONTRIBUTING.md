# راهنمای مشارکت

از اصلاح خطاها و افزودن سناریوهای تازه استقبال می‌کنیم. این مخزن با هدف ارزیابی شفاف و مسئولانه نگهداری می‌شود؛ بنابراین کیفیت و قابلیت بازبینی از تعداد پرامپت‌ها مهم‌تر است.

## پیش از ارسال

1. مسئله یا پیشنهاد خود را در یک Issue توضیح دهید و دستهٔ مناسب را مشخص کنید.
2. مطمئن شوید سناریو با یکی از ابعاد تعریف‌شده در `METHODOLOGY.md` قابل بررسی است.
3. از اطلاعات شخصی واقعی، محتوای دارای حق نشر طولانی، اسرار تجاری و ادعاهای اثبات‌نشده استفاده نکنید.
4. نام تجاری را فقط وقتی برای سناریو ضروری است بیاورید و هیچ وابستگی یا تأییدی القا نکنید.

## قالب رکورد

هر خط `data/prompts.fa.jsonl` باید یک شیء JSON مستقل و منطبق با `schema/prompt.schema.json` باشد. شناسه را به شکل `fa-<category>-<number>` بسازید و نسخه را برای رکورد تازه `1.0.0` بگذارید.

پرامپت خوب:

- یک رفتار مشخص و قابل مشاهده را هدف می‌گیرد؛
- بافت کافی دارد اما پاسخ را تلقین نمی‌کند؛
- برای فارسی یا کاربران فارسی‌زبان ارزش ویژه‌ای دارد؛
- معیارهای بازبینی روشن و مستقل از برند دارد؛
- پاسخ مدل، نتیجهٔ اجرا یا نمرهٔ ازپیش‌تعیین‌شده ندارد.

## بررسی محلی

```bash
python scripts/validate.py
python -m unittest discover -s tests -v
```

هر دو فرمان باید بدون خطا تمام شوند. Pull Request باید هدف، دسته، ریسک احتمالی و دلیل فارسی‌محور بودن سناریو را توضیح دهد.

## تغییرات روش‌شناسی

تغییر ابعاد، مقیاس امتیاز یا قرارداد داده می‌تواند نتایج قدیمی را ناسازگار کند. چنین تغییرهایی باید دلیل روشن، نمونهٔ مهاجرت و افزایش نسخه داشته باشند.

## مجوز مشارکت

با ارسال مشارکت تأیید می‌کنید که حق انتشار آن را دارید و آن را تحت مجوز CC BY 4.0 این مخزن ارائه می‌کنید. مشارکت با کمک هوش مصنوعی مجاز است، اما باید بازبینی انسانی شود و مسئولیت صحت و اصالت آن با مشارکت‌کننده است.
